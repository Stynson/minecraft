#include "cullingSystem.h"

namespace mc {
	core::Vector<Chunk> CullingSystem::getCulledChunks(float x, float y, float direction)
	{
		auto culledChunks = core::Vector<Chunk>();

		return culledChunks;
	}
}